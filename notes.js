// this can be used for simple arguments pushed to the terminal, 
// however commander is more easy and allows us to retrieve a value

// if(process.argv[2] == 'generate'){
//     console.log('Generated')
// }

// this is taking in the user arguments
// program.command('generate')
// .action(() =>{
//     console.log('Generated')
// })

// This option is used for the length arg
// --l, length can be used in the terminal
//<number> specifies the type, for the length needs to take a value, in our case its a number, normally is a boolean
// next value is for the help menu
// the final value is the default, so if no arg is added, 10 is the default value 
